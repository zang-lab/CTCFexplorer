apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ctcf-bw-pvc
spec:
  storageClassName: zanglab-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: ctcf-bw-pv