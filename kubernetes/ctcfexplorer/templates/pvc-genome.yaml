apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ctcf-genome-pvc
  labels:
    app: ctcfexplorer
spec:
  storageClassName: zanglab-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  volumeName: ctcf-genome-pv